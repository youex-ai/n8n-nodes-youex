# Publishes this n8n community node package to npm on every version tag push.
#
# Starting May 1 2026, n8n requires all community nodes to be published via
# GitHub Actions with npm provenance statements. This workflow satisfies that
# requirement. Provenance lets anyone cryptographically verify that a package
# was built by this exact workflow, from this exact repository and commit.
#
# ─── PREREQUISITES ─────────────────────────────────────────────────────────────
#
# @n8n/node-cli ≥ 0.23.0 is required. Earlier versions do not support the
# provenance flag passed by `npm run release` in CI, and the publish step will
# fail. Check the version installed in this project with:
#
#   npm list @n8n/node-cli
#
# To upgrade, add @n8n/node-cli@latest as a dev dependency using your package manager.
#
# ─── ONE-TIME SETUP ────────────────────────────────────────────────────────────
#
# Option A — OIDC Trusted Publishing (recommended, no long-lived secrets):
#   1. Log in to npmjs.com and open your package's settings.
#   2. Under "Publish access" → "Trusted Publishers", click "Add a publisher".
#   3. Select GitHub Actions and fill in:
#        Repository owner: <your-github-username-or-org>
#        Repository name:  <your-repo-name>
#        Workflow name:    publish.yml
#        Environment:      (leave blank unless you use GitHub Environments)
#   4. Leave the N8N_AT_YOUEX secret unset in this repository. GitHub's OIDC
#      token is used instead — no secret ever touches your repo settings.
#
# Option B — npm Automation Token (fallback):
#   1. On npmjs.com: Access Tokens → Generate New Token → Granular Access Token.
#      Scope it to this package with "Read and write" publish permission.
#   2. In GitHub: Settings → Secrets and variables → Actions → New secret.
#      Name it N8N_AT_YOUEX and paste the token value. The name is arbitrary —
#      it only has to match the `secrets.` reference in the Release step below.
#
# Both options work with --provenance. Provenance is signed by GitHub's OIDC
# infrastructure regardless of how npm authentication is handled.
#
# ─── TAG CONVENTION ────────────────────────────────────────────────────────────
#
# This workflow triggers on tags matching "*.*.*" (e.g. 0.2.0).
# If you prefer tags with a "v" prefix (e.g. v0.2.0), change the filter to:
#   tags: ['v*.*.*']
#
# ─── RELEASE PROCESS ───────────────────────────────────────────────────────────
#
# Run the following command locally to start an interactive release:
#
#   npm run release
#
# This will lint, build, prompt for a version bump, update the changelog,
# commit, tag, and push — which triggers this workflow to publish to npm.

name: Publish

on:
  push:
    tags:
      # Matches 0.1.0, 1.2.3, 2.0.0-rc.1, etc.
      # See "TAG CONVENTION" above if you prefer tags with a "v" prefix.
      - '*.*.*'

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest

    permissions:
      # Required to mint an OIDC token for npm provenance attestation.
      id-token: write
      # Scoped down from the default (write) for least-privilege publishing.
      contents: read

    steps:
      - uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          # Pinned to match ci.yml: a release must be built on the Node version the
          # package was tested on, not on whatever `lts/*` resolves to that day.
          node-version: '22'
          cache: 'npm'

      # npm's OIDC exchange for trusted publishing needs npm >= 11.5.1, and the
      # npm bundled with Node 22 is still on 10.x. Upgrading explicitly keeps this
      # independent of whichever npm the runner image happens to ship.
      - name: Use an npm that can do trusted publishing
        run: |
          npm install -g npm@latest
          node --version
          npm --version

      - name: Install dependencies
        run: 'npm ci'

      - name: Release
        # Publishes to npm with a provenance attestation (requires id-token: write above).
        # Lint and build run automatically as part of the release process.
        #
        # Option A (OIDC): leave N8N_AT_YOUEX unset — the token step is skipped and
        #   npm uses the OIDC exchange instead.
        # Option B (token): set N8N_AT_YOUEX in repo secrets — it is written to
        #   .npmrc here before publishing.
        run: |
          # Both auth paths stay supported, but which one was taken has to be
          # visible. Without this the token being absent is silent: the `&&`
          # short-circuits, the run carries on through lint, build and packing,
          # and only dies at the very end with a bare ENEEDAUTH that says
          # nothing about the secret.
          if [ -n "$NPM_TOKEN" ]; then
            echo "::notice::Authenticating to npm with the repository token secret."
            npm config set //registry.npmjs.org/:_authToken "$NPM_TOKEN"
          else
            echo "::notice::No token secret present - relying on npm trusted publishing (OIDC)."
          fi

          # npm's own failure text is the only thing that identifies which of
          # npm's several publish policies rejected the release, and reading it
          # otherwise means opening the run in a browser. Surfacing the tail as
          # annotations puts it in the API, where tooling and reviewers can see
          # it without clicking through.
          set +e
          npm run release 2>&1 | tee /tmp/release.log
          status=${PIPESTATUS[0]}
          set -e
          if [ "$status" -ne 0 ]; then
            echo "::error::npm publish failed with exit code $status. Tail of the log:"
            grep -E "npm error|npm warn|npm notice Publishing" /tmp/release.log | tail -n 12               | while IFS= read -r line; do echo "::error::$line"; done
            exit "$status"
          fi
        env:
          # The repository secret is named N8N_AT_YOUEX; it is mapped onto the
          # NPM_TOKEN variable the step below reads. Renaming the secret is not
          # possible in GitHub, and recreating it would mean regenerating the npm
          # token, since a secret's value cannot be read back.
          NPM_TOKEN: ${{ secrets.N8N_AT_YOUEX }}
